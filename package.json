{
  "name": "architect-portfolio",
  "version": "1.0.0",
  "description": "Precision Modernist Portfolio",
  "main": "index.html",
  "scripts": {
    "build:css": "npx tailwindcss -i src/style.css -o style.css --minify",
    "start": "npx -y http-server -p 8080"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1",
    "@tailwindcss/forms": "^0.5.7",
    "@tailwindcss/container-queries": "^0.1.1"
  }
}
